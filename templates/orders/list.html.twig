{% extends 'base.html.twig' %}

{% block nav %}
    nav administration
{% endblock %}

{% block main %}
    {{ title }}
    {% for item in orders %}
        {{ item.id }} {{ item.state.label }} {{ item.total }}
        {% for orderLine in item.orderLines %}
            {{ orderLine.total }} {{ orderLine.product_name }}
        {% else %}
            Aucune catégorie existante
        {% endfor %}
    {% endfor %}
{% endblock %}

{% block footer %}
{% endblock %}